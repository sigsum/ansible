---
- name: Converge
  hosts: all
  become: true
  roles:
    - sigsum.tlog.sigsum_agent
  vars:
    sigsum_yubihsm_credentials:
      - { port: "12345", auth_id: "200", passphrase: "allan tar kakan i 1200 baud" }
