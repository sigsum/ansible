---
# Whether to enforce the Sigsum project's minimum Go version
golang_ensure_min_version: false

# Debian options
golang_debian_deb_source: https://ftp.acc.umu.se/debian
golang_debian_use_backports: false
