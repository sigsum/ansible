---
- name: Install yubihsm-connector
  ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] }}.yml"

- name: Configure yubihsm-connector, one file for each yubihsm-connector instance
  ansible.builtin.copy:
    content: |
      # This file is managed by ansible -- do not edit
      listen: "127.0.0.1:{{ item.port }}"
      serial: "{{ item.serial_number }}"
      log-level: {{ yubihsm_connector_log_level }}
    dest: "/etc/yubihsm-connector-{{ item.name }}.yaml"
    mode: '0644'
  with_items: "{{ yubihsm_connector_instances }}"

- name: Create yubihsm-connector systemd instantiated service file
  ansible.builtin.copy:
    dest: /etc/systemd/system/yubihsm-connector@.service
    src: etc/systemd/system/yubihsm-connector@.service

- name: Create yubihsm-connector-manager.service file
  ansible.builtin.template:
    dest: /etc/systemd/system/yubihsm-connector-manager.service
    src: etc/systemd/system/yubihsm-connector-manager.service.j2

- name: Enable yubihsm-connector-manager service
  ansible.builtin.systemd_service:
    name: yubihsm-connector-manager
    enabled: true
    state: started
