Install molecule and the podman plugin:

    $ pip install molecule molecule-podman

The system also needs ansible and podman to be installed.

Run the tests:

    $ molecule test

See molecule/default/molecule.yml for additional hints.
