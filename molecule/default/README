We test that the primary log is up and running, including end-to-end tests like
submitting checksums, requesting proofs, and verifying that these proofs are OK.

We currently don't do any testing with a secondary.
